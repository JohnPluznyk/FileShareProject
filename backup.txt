const http = require('http');
const fs = require('fs');
const path = require('path');

const directoryPath = "/";
// TODO: Figure out why that semicolon on the last line is underlined red pretty
// TODO: Figure out how to serve the index.html file to the user
// TODO: Take the contents of the files and display them within the html


// Create a server
const server = http.createServer((req, res) => {
    fs.readdir(directoryPath, (err, files) => {
        if (err) {
          console.error('Error reading directory:', err);
          return;
        }

        // Log all files in the directory
        console.log('Files in the directory:');
        files.forEach(file => {
            console.log(file);
        });
    });
});    

// Listen on port 3000
server.listen(3000, () => {
    console.log('Server running at http://localhost:3000/');
});  